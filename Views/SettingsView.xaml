<Window x:Class="CollisionControl.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CollisionControl"
             xmlns:p="clr-namespace:CollisionControl.Properties"
             mc:Ignorable="d" 
            
            Height="400"
        MinHeight="400"
            Width="400"
         MinWidth="400"
            Title="Редактирование профилей">
    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Button.BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0" SpreadMethod="Pad" Opacity="1">
                        <GradientStop Color="#FF233C91" Offset="0.6" />
                        <GradientStop Color="#FFB455A0" Offset="1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Style.Setters>
                <Setter Property="BorderBrush" Value="MediumPurple"/>
                <Setter Property="BorderThickness" Value="2"/>
            </Style.Setters>
        </Style>
        <Style x:Key="WindowStyle" TargetType="{x:Type Window}">
            <Setter Property="Window.BorderBrush">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0" SpreadMethod="Pad" Opacity="1">
                        <GradientStop Color="#FF233C91" Offset="0.6" />
                        <GradientStop Color="#FFB455A0" Offset="1" />
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
        <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"  BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!--<Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background"  Value="#1F26A0DA"/>
                                            <Setter Property="BorderBrush"  Value="#a826A0Da"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background"  Value="#3D26A0DA"/>
                                            <Setter Property="BorderBrush"  Value="#FF26A0DA"/>
                                        </Trigger>-->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#FF233C91" Opacity="0.2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,1" EndPoint="1,0" SpreadMethod="Pad" Opacity="0.5">
                                            <GradientStop Color="#FF233C91" Offset="0.6" />
                                            <GradientStop Color="#FFB455A0" Offset="1" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="BorderBrush">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,1" EndPoint="1,0" SpreadMethod="Pad" Opacity="1">
                                            <GradientStop Color="#FF233C91" Offset="0.6" />
                                            <GradientStop Color="#FFB455A0" Offset="1" />
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="170"/>
        </Grid.ColumnDefinitions>
        <GroupBox Header="Путь к настройкам" Grid.Row="0" Margin="2" Grid.ColumnSpan="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="45"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Margin="5"
                           
                           ToolTip="{Binding Source={x:Static p:Settings.Default}, Path=SettingPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                           Text="{Binding Source={x:Static p:Settings.Default}, Path=SettingPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content=" . . . "
                        Margin="5"
                        Grid.Column="1"
                        Command="{Binding ChangePathCommand}"
                         Style="{StaticResource ButtonStyle}">

                </Button>
            </Grid>
        </GroupBox>

        <GroupBox Header="Профили" Grid.Row="1" Margin="2"  Grid.RowSpan="2">
            <ListBox
                 Grid.Row="0"
                 Margin="5"
                ItemsSource="{Binding Profiles}"
                SelectedItem="{Binding SelectedProfile,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                 SelectionMode="Single"
                
                DisplayMemberPath="Name"
                 ItemContainerStyle="{StaticResource ListBoxItemStyle}">
            </ListBox>

        </GroupBox>
        <GroupBox Header="Редактирование" Grid.Column="1" Grid.Row="1" Margin="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button 
                        Margin="5"
                    Grid.Row="0"
                        Command="{Binding AddCommand}"
                     Style="{StaticResource ButtonStyle}">
                    <TextBlock TextAlignment="Center">Добавить <LineBreak/> текущее состояние</TextBlock>
                </Button>
                <Button Content="Удалить"
                        Grid.Row="1"
                        Margin="5"
                        Command="{Binding DeleteCommand}"
                         Style="{StaticResource ButtonStyle}">
                </Button>
                <Button  Content="Изменить название"
                         Grid.Row="2"
                         Margin="5"
                         Command="{Binding ChangeNameCommand}"
                         Style="{StaticResource ButtonStyle}">
                </Button>
                <Button 
                         FontFamily="Segoe UI Symbol" Content="&#xE110;"
                         Margin="5" 
                         Command="{Binding UpCommand}" 
                         Grid.Row="3"
                         Style="{StaticResource ButtonStyle}">
                </Button>
                <Button 
                         FontFamily="Segoe UI Symbol" Content="&#xE1FD;"
                         Margin="5" 
                         Command="{Binding DownCommand}" 
                         Grid.Row="4"
                         Style="{StaticResource ButtonStyle}"/>
                <Button Content="Сохранить и выйти"
                        Grid.Row="6"
                        Margin="5"
                        Command="{Binding SaveAndExitCommand}"
                        Style="{StaticResource ButtonStyle}">
                </Button>
            </Grid>
        </GroupBox>

        <!-- Дополнительные элементы управления могут быть добавлены сюда -->
    </Grid>
</Window>
